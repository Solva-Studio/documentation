# Commands : work
## Usages :
- `<prefix>work` : Every 10 minutes, the player can run the command to receive a certain quantity of coins

`<prefix>` = Current bot prefix (default : `!`)

## System Logic :
### Reward :
The **reward** can range from **100** to **200 🪙 coins**.
