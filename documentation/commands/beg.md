# Commands : beg
## Usages :
- `<prefix>beg` : Every hour, the player can run the command to receive a certain quantity of coins

`<prefix>` = Current bot prefix (default : `!`)

## System Logic :
### Reward :
The **reward** can range from **1** to **100 🪙 coins**.
